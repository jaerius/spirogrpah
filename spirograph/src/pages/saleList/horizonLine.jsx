import React from "react";

const HorizonLine = ({ text }) => {
  return (
    <div
      style={{
        // display: "block",
        width: "100%",
        textAlign: "center",
        borderBottom: "1px solid #333",
        lineHeight: "0.1rem",
        margin: "0.5rem",
        overflow: "hidden",
      }}
    ></div>
  );
};

export default HorizonLine;
